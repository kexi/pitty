/target
/logs
/result.*

# Per-scenario logs emitted when running scenarios (write_log writes to the
# scenario's own directory). Keep the scenarios tracked, ignore their output.
e2e/**/logs/
examples/**/logs/

# direnv
.direnv

# nix build outputs
result
result-*
