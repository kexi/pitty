/target
/logs
/result.*
Cargo.lock

# Per-scenario logs emitted by dogfood runs under e2e/ (write_log writes to the
# scenario's own directory). Keep the scenarios tracked, ignore their output.
e2e/**/logs/

# direnv
.direnv

# nix build outputs
result
result-*
